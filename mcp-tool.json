{
  "mcpServers": {
    "crm-assistant": {
      "command": "node",
      "args": ["server.js"],
      "env": {
        "SUPABASE_URL": "${SUPABASE_URL}",
        "SUPABASE_KEY": "${SUPABASE_KEY}"
      }
    }
  }
}
